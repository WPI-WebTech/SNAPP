import React, { Component } from 'react';

/**
 * Screen that displays the status of a user's request
 */
class StatusScreen extends Component {
    render() {
        return (
            <div>
                Status Screen (view request status)
            </div>
        );
    }
}

export default StatusScreen;

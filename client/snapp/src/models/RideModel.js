export default (
    fromAddress,
    toAddress,
    numPassengers,
    accommodations,
    status
) => {
    return {
        fromAddress,
        toAddress,
        numPassengers,
        accommodations,
        status
    }
}
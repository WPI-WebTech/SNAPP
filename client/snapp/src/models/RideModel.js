export default (
    fromAddress,
    toAddress,
    numPassengers,
    accommodations
) => {
    return {
        fromAddress,
        toAddress,
        numPassengers,
        accommodations
    }
}